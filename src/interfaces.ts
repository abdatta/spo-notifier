export interface DashboardPost {
    title: string;
    date: string;
    body: string;
}

export interface Subscriber {
    id: string,
    email: string
}

export interface JobOpening {
    id: string;
    company: string;
    profile: string;
}
